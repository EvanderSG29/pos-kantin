# Git Workflow

- This project targets a private GitHub repository under `EvanderSG29/pos-kantin`.
- Use `main` as the default branch.
- Prefer small commits after each meaningful slice of work.
- Before editing, check `git status`.
- Before pushing, review `git diff` or `git status`.
- Do not commit `.clasp.json`, secret values, or live deployment URLs.

